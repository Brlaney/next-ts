export interface IVials {
  id: number;
  slots: number[];
  status: number[];
};

export interface IPublications {
  date: string;
  title: string;
  link: string;
  bpr: string;
  attorney: string;
};
